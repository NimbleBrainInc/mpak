# registry.mpak.dev — the mpak registry application.
# Marketing and documentation live at mpak.dev and have their own robots.txt.

User-agent: *
Allow: /
Disallow: /login
Disallow: /my-packages

# Package pages are the indexable surface here and are generated from the
# registry itself, so this sitemap can never drift from what actually exists.
Sitemap: https://registry.mpak.dev/sitemap-packages.xml
